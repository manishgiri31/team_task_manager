/**
 * Cookie / storage names only (no env reads).
 * Imported by middleware (Edge) — keep free of Node/browser-only APIs.
 */
export const SESSION_COOKIE_NAME = "tm_session";
export const TOKEN_STORAGE_KEY = "tm_access_token";
